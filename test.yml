---
- name: Manage RouterOS
  ansible_host: 10.0.105.68
  gather_facts: no
  collections:
    - community.routeros
  tasks:
    - name: Execute a command on RouterOS
      community.routeros.command:
        commands:
          - /system identity print
