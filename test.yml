- hosts: all
  collections:
    - community.routeros

- name: Run command on remote devices

- tasks:

    - name: system command

      community.routeros:

        commands: /system routerboard print

      register: system_print
